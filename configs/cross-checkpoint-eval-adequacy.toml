# Full decoupled model arm: adequacy-gated defense plus adequacy-damped game
# evidence, on the shared arm recording settings.
extends = "hybrid-decoupled.toml"

[simulation]
record_task_records = true
task_record_sample_rate = 0.001
