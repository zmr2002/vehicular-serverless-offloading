# Legacy reported results

These files were used when writing the submitted thesis and are preserved unchanged for provenance. They contain inconsistent number formatting and were produced by legacy scripts that are not treated as the executable source of truth.

Do not overwrite these files. Corrected runs belong under `../verified/`.
